#include <stdio.h>
#include "main.h"
/**
 * main - prints the numberof the argument passed into the program
 *
 * @argc: argument count.
 * @argv: argument vector.
 *
 * return: always 0.
 */
int main(int argc, char *argv[])
{
	argv = argv;
	printf("%d\n", argc - 1);
	return (0);
}
