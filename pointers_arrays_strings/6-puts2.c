#include "main.h"
/**
 * main - prints every other character of a string starting 1 character.
 *
 * Return: Always 0.
 */

void puts2(char *str)
{
int length;
int i;

	for (length = 0; str[length] != '\0'; length++)
	{

	}

	for ( i = length; i <= 8; i * 2)
	{
	
	}


