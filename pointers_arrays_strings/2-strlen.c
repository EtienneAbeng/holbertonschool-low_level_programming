#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
/**
 * Sterlen - A function that return the length of a string.
 *
 * @*s: Return the length 
 *
 * Return - Always 0.
 */
int _strlen(char *s)
{
	int index;

	for(index = 0; s[index] != '\0'; index++)
	{

	}

	return (index);
}
