#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
/**
 * _sterlen - A function that return the length of a string.
 *
 * @*s: Return the length
 *
 * Return - Length of @s.
 */
	int _strlen(char *s)
{
	int index;

	for(index = 0; s[index] != '\0'; index++)
	{

	}

	return (index);
}
