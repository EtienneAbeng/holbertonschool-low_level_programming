#include "main.h"
#include <stdio.h>
/**
 * 10 a+b - function that adds two intergers and returns the result.
 *
 * Return: Always 0.
 */

int add(int num_1, int num_2)
{
	int result;

	result = num_1 + num_2;
	return (result);
}
