#include "main.h"
#include <stdlib.h>
#include <stdio.h>
/**
 * Abs - There is no such thing as absolute calue in this world.
 *
 * return: Always 0.
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}

	else if (n == 0)
	{
		return (n);
	}

	else
	{
		return (n);
	}
}
