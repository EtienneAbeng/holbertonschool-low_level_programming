#include "main.h"
/**
 * Last digit - A function that print the last digit of a number.
 *
 * Return: the value of the last digit.
 */

int print_last_digit(int n)
{

	if (n % 10 > 0)
	{
		_putchar(n);
	}

	else
	{
		_putchar(n);
	}

	return (n);
}
